apiVersion: v1
kind: Secret
metadata:
  name: mongodb-secret
type: Opaque
data:
  MONGO_INITDB_ROOT_USERNAME: YWRtaW4=  # admin (without newline)
  MONGO_INITDB_ROOT_PASSWORD: cGFzcw==  # pass (without newline)